# Rules

## Markdown Paths

Planning and design Markdown documents must use repo-relative paths. Do not use
machine-local absolute paths, workspace paths, or home-directory aliases.

## Document Overwrites

When overwriting a planning or design document, include a revision history that
states the source inputs, major changes, and superseded assumptions or decisions.
